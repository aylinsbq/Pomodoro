<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pomodoro" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Pomodoro" parent="Base.Theme.Pomodoro">
        <item name="colorPrimary">@color/primary</item>

    </style>

<!--Remove Splash Default Screen Theme-->
    <style name="Theme.RemoveSplashScreenTheme" parent="@style/Theme.Pomodoro">
        <item name="android:windowIsTranslucent">true</item>
    </style>

<!--NumberPicker Style-->
    <style name="NumberPickerCustomText">
        <item name="android:textSize">32sp</item>
        <item name="android:fontFamily">@font/langar</item>
        <item name="android:textColorPrimary">@color/text_color</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="colorControlNormal">@color/primary_light</item>
    </style>

    <!--Text Dialog New Pomodoro Style-->
    <style name="DialogNewPomodoroCustomText">
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">@font/langar</item>
        <item name="android:textColor">@color/text_color</item>
    </style>


    
</resources>